akka {
  loglevel = DEBUG
}

http {
  host = "0.0.0.0"
  port = 9000
}

# Cassandra Cluster Configuration
#
# host = ["host1", "host2", "host3]
# keyspace = The keyspace name you want to connect
# username and password are by default "cassandra", edit to match your setup
cassandra {
  host = ["192.168.99.100"]
  keyspace = "testtask"
  username = "cassandra"
  password = "password123"
}